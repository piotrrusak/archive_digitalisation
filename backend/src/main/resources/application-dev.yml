spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/postgres_backend
    username: postgres_backend
    password: password
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: update
